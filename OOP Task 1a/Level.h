#pragma once

class Level
{
	public:
        // constructor
        Level();

        // assessors (Getters) check for const

        // mutators (Setters) NO CONST

private:
    // data members
    int levelNum;
    
};

